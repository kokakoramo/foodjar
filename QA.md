# v6 個人保存版驗收

## 已執行

- 讀取原 repository 全部九個檔案，基於原 main 的 1f1c74a 修改。
- 六項 Node 邏輯／資產測試通過，詳見 tests/p0.test.cjs。
- app.js、cloud.js、storage.js、sw.js 語法檢查。
- 所有 HTML 靜態檔案引用存在；Manifest 圖示 PNG 尺寸正確。

## 尚未執行，發布前需要完成

目前沒有完成實際桌機／手機瀏覽器 QA；不可把以上測試當作 console 或視覺驗收。
正式 GitHub Pages 的讀取亦未成功，不宣稱正式網站已更新。

1. 在原 GitHub Pages 網址使用的同一瀏覽器，先匯出 v6 備份。
2. 桌面及 iPhone Safari（含加入主畫面）開啟新版：直接進入 Jar，不顯示登入。
3. 舊餐點、照片、主題保留；全新瀏覽器應無示範餐點。
4. 新增附照片餐點，切換 Diary / Recap，比對金額；重新整理、關閉再開後檢查仍保留。
5. 編輯後重新開啟；刪除後重新開啟；確認相關統計一致。
6. 相簿、相機、透明 PNG；快速換圖或關閉表單後，不得串到下一餐。
7. 月份 A 設 5000、B 設 7000；來回切換並重新開啟，確認各自保留。
8. 匯出後在另一瀏覽器匯入，確認照片／金額／各月預算一致；無效備份不得取代資料。
9. 同時開兩個分頁，A 更新後 B 儲存應提示重新整理，不可覆蓋 A 的紀錄。
10. 首次連网安裝後關閉全部分頁，再以標準網址開啟，離線重開並新增／編輯／刪除。
11. 開發者工具 Console 無 error；Network 無 broken asset；Cache Storage 沒有私人 API 資料。
12. 手機 320、390、430 px：各頁、照片與表單、鍵盤開啟、月份選擇、底部導覽無遮擋。

## 邊界

無登入、無跨裝置同步；資料清除仍需 JSON 備份恢復。
AI 去背、縮圖、物理堆疊及語意拆分不在本次已完成範圍。
