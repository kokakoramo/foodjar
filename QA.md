# v6 個人保存版驗收

## 已執行（2026-09-07）

- 讀取原 repository 全部九個檔案，延續原 main 的 1f1c74a。
- 六項 Node 邏輯／資產測試通過，詳見 tests/p0.test.cjs。
- JavaScript 語法檢查、HTML 靜態引用存在、Manifest PNG 尺寸正確。
- Chromium 桌面預覽：免登入空罐、新增餐點、重新整理保留紀錄。
- 390 px iframe 預覽：Jar、Diary、編輯 120 → 150、Recap 對應更新。
- 實際選擇 PNG 檔、儲存餐點並重新整理後保留照片。
- 9 月預算 5000、10 月 7000 分開保存；重新整理後 9 月仍為 5000。
- 實際下載 JSON，檢查包含一筆 120 元測試餐點、嵌入 PNG 及上述兩個月預算。

## 尚待實測

- 刪除與還原備份完整流程：測試工具卡在原生確認視窗，不能宣稱通過。
- 兩個分頁的寫入衝突、舊版真實資料遷移、儲存配額不足。
- 320 / 430 px 全頁面；真實 iPhone Safari、鍵盤、相機與加入主畫面。
- HTTPS Service Worker 安裝及離線重開／CRUD。HTTP 預覽未提供 Service Worker，介面正確顯示未啟用離線功能。
- 完整 Console 與 Network 驗收尚未完成；測試工具本身的擴充套件錯誤不列為網站錯誤。

上述邏輯測試不等同真實裝置或離線端到端驗收。手機 iframe 是 Chromium 響應式檢查，並非 iPhone Safari。

## 後續回歸步驟

1. 舊版先匯出備份，更新後關閉所有 Food Jar 分頁再開啟。
2. 檢查舊餐點、照片、主題；新增、編輯、刪除並重開，比對 Diary / Recap。
3. 匯出後於另一瀏覽器還原，比對照片、金額與各月預算；無效備份不得取代資料。
4. A 分頁更新後，B 儲存應提示重新整理，不覆蓋 A。
5. HTTPS 首次連網安裝成功後，離線重開、新增／編輯／刪除。
6. iPhone Safari 及主畫面版檢查相簿、鍵盤、月份選擇與底部導覽。

## 邊界

無登入、無跨裝置同步；清除網站資料仍需 JSON 備份恢復。
AI 去背、縮圖、物理堆疊及語意拆分不在本次已完成範圍。

## 2026-09-08 罐子互動更新

- 八項 Node 測試通過：新增實際 Matter 引擎 14 個物件穩定堆疊／邊界／穿透深度檢查，以及新餐點落下、縮放座標拖曳、拖曳不開詳情、月份切換清理。
- Chromium 390 px 預覽實際上傳 PNG、保存後觀察位置從罐口負座標落到罐底；拖曳放開未誤開詳情。
- 320 px 視覺檢查修正罐子偏右裁切；430 px 無橫向溢出。
- 輕點無照片餐點後再開照片餐點，詳情圖片正常。Recap 2 餐共 340、平均 170、最高 220 正確。
- 音效開關狀態可切換；Console 未見網站 error，頁面圖片無載入失敗。
- 未以真實 iPhone 實測觸控與聆聽音效，HTTPS 離線流程仍待實測。

## 保存保護與中央入罐更新

- 13 項自動測試通過，包括重新載入模組、主資料缺失時由副本還原、故意刪除不復活、讀取失敗不回傳空資料、副本配額不足仍保留主資料。IndexedDB 測試使用 fake-indexeddb，非真實裝置。
- Chromium 真實關閉測試分頁後另開分頁，3 筆餐點及照片保留，合計 460；先前版本的 2 筆測試資料也在更新後保留。
- 390 px 檢查金額下方快捷餐別；選飲料成功保存為飲料。中央顯示 340 +120，完成後總額 460。
- 頁面更新後未见網站 Console error；真實 iPhone 的資料消失原因尚未重現，也未宣稱已找出根因。整個網站儲存被清除仍需外部備份。

- 2026-09-09：依真實 iPhone 截圖放大罐子，物理範圍由 262×304 增至 302×376；保留 320 px 縮放規則。
